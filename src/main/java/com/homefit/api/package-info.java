/**
 * api — request-time HTTP surface. Reads the precomputed read model and applies user weights.
 * May depend only on {@code core}.
 */
@org.springframework.modulith.ApplicationModule(displayName = "api", allowedDependencies = "core")
package com.homefit.api;
